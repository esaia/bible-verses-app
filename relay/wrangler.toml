name = "mybible-relay"
main = "src/index.js"
compatibility_date = "2026-01-01"

[[durable_objects.bindings]]
name = "ROOMS"
class_name = "Room"

# SQLite-backed Durable Objects are the only kind available on the Workers
# free plan, and the default for new classes.
[[migrations]]
tag = "v1"
new_sqlite_classes = ["Room"]
