# https://www.robotstxt.org/robotstxt.html

User-agent: *
Allow: /

# Output surfaces, not pages: /show is the projector canvas and /lower3rd is
# the OBS browser source. Neither means anything without the window driving it.
Disallow: /show
Disallow: /lower3rd

# /studio stays crawlable on purpose. The card crawlers — Facebook's among
# them — honour this file, and a disallowed page gets no link preview at all.
# It carries noindex in its own head instead, so it is shareable without
# turning up in results.

Sitemap: https://bibleversesgeo.netlify.app/sitemap.xml
